import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'About Us',
    description: 'Learn more about RNG Gamez, your premier Local Game Store (LGS) in South Plainfield, NJ. We specialize in card tournaments, singles, and sealed trading card games.',
};

export default function About() {
    return (
        <main>
            {/* About Page */}
        </main>
    );
}
